//your JS code here. If required.
const info = document.getElementById('sizeinfo').firstElementChild;
function getinfo(){
	info.innerHTML = `Width: ${window.innerWidth} and Height: ${window.innerHeight}`
}
